// server/routes/adminRoutes.ts
import { Router } from 'express';
// import { getAllUsers, deleteUser,  } from '../controllers/admin'; // Importer les contrôleurs

const router = Router();

// Définir les routes d'administration
// router.get('/users', getAllUsers); // Route pour obtenir tous les utilisateurs
// router.get('/posts', getAllPosts); // Route pour obtenir tous les posts
// router.delete('/users/:id', deleteUser); // Route pour supprimer un utilisateur par ID
// router.delete('/posts/:id', deletePost); // Route pour supprimer un post par ID
// router.get('/stats', getStatistics); // Route pour obtenir les statistiques

export default router;
